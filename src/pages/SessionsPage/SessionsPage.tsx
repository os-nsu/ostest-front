import DefaultPageLayout from '@/components/DefaultPageLayout/DefaultPageLayout.tsx';
import SessionsPageContent from '@/components/SessionsPageComponents/SessionsPageContent';

export default function SessionsPage() {
  return (
    <DefaultPageLayout>
      <SessionsPageContent />
    </DefaultPageLayout>
  );
}
